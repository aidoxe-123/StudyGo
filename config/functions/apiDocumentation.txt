url: https://fir-tut2-82e4f.firebaseapp.com/api/v1

POST /register
    req: email, password
    res: emailUsed (whether or not the email is used for another account)

POST /login
    req: email, password
    res: accExist (whether or not an account with the given email is exist)
         passwordCorrect (whether or not the password is correct)
         userId 
         
GET /users/:id
    res: idValid (whether or not the given ID is valid)
         email 
         password   